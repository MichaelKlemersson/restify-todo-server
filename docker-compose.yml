version: '2'

services:
  server:
    build: .
    container_name: 'restify-todo-app-server'
    volumes:
      - .:/var/www/app
    command: bash -c 'npm install && npm run serve'
    ports:
      - "8080:3000"
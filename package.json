{
  "name": "restify-todo-server",
  "version": "1.0.0",
  "description": "a simple backend made in node with restify for a todo app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "npm run clean && webpack",
    "dev": "NODE_ENV=development npm run build && node --harmony dist/server.js"
  },
  "keywords": [
    "restify",
    "todo"
  ],
  "author": "michael klemersson",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.25.0",
    "babel-loader": "^7.1.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-env": "^1.5.2",
    "copy-webpack-plugin": "^4.0.1",
    "webpack": "^3.0.0",
    "webpack-node-externals": "^1.6.0"
  },
  "dependencies": {
    "co": "^4.6.0",
    "mongoose": "~4.10.*",
    "restify": "~4.3.*",
    "restify-errors": "~4.3.*",
    "restify-router": "~0.4.*"
  }
}
